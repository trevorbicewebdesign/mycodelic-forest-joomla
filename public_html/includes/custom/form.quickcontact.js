// JavaScript Document
window.addEvent('domready', function(){
	/*
	$$('.quickcontact #phone').each(function(input){
		input.meiomask('fixed.phone-us', JSON.decode('{autoTab: true}'));
	});
	*/
});

function onFormSubmitQuickContact() {
	
}